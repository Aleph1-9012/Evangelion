#!/bin/sh
# SPDX-License-Identifier: Apache-2.0
set -eu
directory=${EVANGELION_GRUB_SOURCE_DIR:?Original GRUB generator directory is missing}
here=$(CDPATH='' cd -- "$(dirname -- "$0")/.." && pwd)
exec bash "$here/boot-console" generate "$directory"
